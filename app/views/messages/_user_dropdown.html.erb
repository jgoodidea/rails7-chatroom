<div class="dropdown">
    <a href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown">
        <%= content_tag(:div, user.username, class: "msg-role-#{user.role}") %>
    </a>
    <ul class="dropdown-menu bg-light">
        <div class="m-2">
            <%= image_tag(user.chat_thumbnail, class: 'chat_pfp') %>
            <%= content_tag(:div, user.username) %>
            <%= content_tag(:p, user.role, class: "msg-role-#{user.role}") %>
        </div>
        <li><hr class="dropdown-divider"></li>
        <li><%= link_to "Message", user, class: "dropdown-item" %></li>
    </ul>
</div>
